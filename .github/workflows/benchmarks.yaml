name: Performance Benchmarks

on:
  # Run benchmarks on demand
  workflow_dispatch:
    inputs:
      benchmark_type:
        description: "Benchmark to run (all, random_access, read_window, index, benchmark)"
        type: choice
        options:
          - all
          - random_access
          - read_window
          - index
          - benchmark
        default: "all"

  # Run benchmarks weekly
  schedule:
    - cron: "0 0 * * 0" # Run at midnight on Sundays

  # Optionally run on PRs with a specific label
  pull_request:
    types: [labeled]

# Ensure only one benchmark job runs at a time
concurrency:
  group: benchmarks
  cancel-in-progress: false

jobs:
  benchmark:
    # Only run on PRs with the 'run-benchmarks' label
    if: github.event_name != 'pull_request' || github.event.label.name == 'run-benchmarks'

    name: Run Benchmarks
    runs-on: self-hosted
    timeout-minutes: 1440 # 24 hours

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Fetch all history for proper benchmarking

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache dependencies
        uses: Swatinem/rust-cache@v2

      - name: Prepare results directory
        run: |
          mkdir -p ./results
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          echo "TIMESTAMP=$TIMESTAMP" >> $GITHUB_ENV
          echo "# Benchmark Results - $(date)" > ./results/summary.md
          echo "## Environment" >> ./results/summary.md
          echo "![runner bare metal host metrics](https://metrics.sui.io/goto/D1MxYcBNg?orgId=1)" >> ./results/summary.md

      - name: Build all benchmarks
        run: cargo build --release --all

      - name: Run random_access_speed_test
        if: github.event.inputs.benchmark_type == 'all' || github.event.inputs.benchmark_type == 'random_access' || github.event_name != 'workflow_dispatch'
        run: |
          echo "## Random Access Speed Test" >> ./results/summary.md
          echo '```' >> ./results/summary.md
          mkdir -p ./tmp_benchmark
          cd ./tmp_benchmark
          cargo run --release --manifest-path ../benchmarks/random_access_speed_test/Cargo.toml | tee ../results/random_access_${{ env.TIMESTAMP }}.txt | tee -a ../results/summary.md
          cd ..
          rm -rf ./tmp_benchmark
          echo '```' >> ./results/summary.md

      - name: Run read_window_test
        if: github.event.inputs.benchmark_type == 'all' || github.event.inputs.benchmark_type == 'read_window' || github.event_name != 'workflow_dispatch'
        run: |
          echo "## Read Window Test" >> ./results/summary.md
          echo '```' >> ./results/summary.md
          cargo run --release --manifest-path benchmarks/read_window_test/Cargo.toml | tee ./results/read_window_${{ env.TIMESTAMP }}.txt | tee -a ./results/summary.md
          echo '```' >> ./results/summary.md

      - name: Generate index benchmark files
        if: github.event.inputs.benchmark_type == 'all' || github.event.inputs.benchmark_type == 'index' || github.event_name != 'workflow_dispatch'
        run: |
          BENCH_DATA_DIR="/opt/sui/db/tidehunter-benchmarks"
          sudo mkdir -p $BENCH_DATA_DIR
          sudo chown $USER $BENCH_DATA_DIR

          # Only generate if files don't exist
          if [ ! -f "$BENCH_DATA_DIR/bench-header-100GB-100K.dat" ] || [ ! -f "$BENCH_DATA_DIR/bench-uniform-file-100GB-100K.dat" ]; then
            echo "Generating benchmark files..."
            cargo run --release --manifest-path benchmarks/index_benchmark/Cargo.toml -- generate \
              --num-indices 25000 \
              --entries-per-index 1000000 \
              --header-file $BENCH_DATA_DIR/bench-header-100GB-100K.dat \
              --uniform-file $BENCH_DATA_DIR/bench-uniform-file-100GB-100K.dat
          else
            echo "Using existing benchmark files from $BENCH_DATA_DIR"
          fi

          # Create symlinks to the data directory
          mkdir -p data
          ln -sf $BENCH_DATA_DIR/bench-header-100GB-100K.dat data/
          ln -sf $BENCH_DATA_DIR/bench-uniform-file-100GB-100K.dat data/

      - name: Run index_benchmark
        if: github.event.inputs.benchmark_type == 'all' || github.event.inputs.benchmark_type == 'index' || github.event_name != 'workflow_dispatch'
        run: |
          echo "## Index Benchmark" >> ./results/summary.md
          echo '```' >> ./results/summary.md
          cargo run --release --manifest-path benchmarks/index_benchmark/Cargo.toml -- run \
            --num-lookups 1000000 \
            --num-runs 1 \
            --batch-size 1000 \
            --window-size 800 \
            --header-file data/bench-header-100GB-100K.dat \
            --uniform-file data/bench-uniform-file-100GB-100K.dat \
            | tee ./results/index_${{ env.TIMESTAMP }}.txt | tee -a ./results/summary.md
          echo '```' >> ./results/summary.md

      - name: Run general benchmark
        if: github.event.inputs.benchmark_type == 'all' || github.event.inputs.benchmark_type == 'benchmark' || github.event_name != 'workflow_dispatch'
        run: |
          echo "## General Benchmark" >> ./results/summary.md
          echo '```' >> ./results/summary.md
          cargo run --release --manifest-path benchmarks/benchmark/Cargo.toml -- \
            --reads 1000000 \
            --writes 1000000 \
            --write-threads 4 \
            --read-threads 4 \
            --write-size 1024 \
            --key-len 32 \
            --background-writes 100000 \
            | tee ./results/benchmark_${{ env.TIMESTAMP }}.txt | tee -a ./results/summary.md
          echo '```' >> ./results/summary.md

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: benchmark-results-${{ env.TIMESTAMP }}
          path: results/
          retention-days: 20

      - name: Comment PR with benchmark results
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require('fs');
            const benchmarkSummary = fs.readFileSync('./results/summary.md', 'utf8');
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: benchmarkSummary
            });

      - name: Create condensed summary for Slack
        id: condensed_summary
        if: github.event_name == 'schedule'
        run: |
          # Extract just the important numbers and results
          {
            echo "# Benchmark Summary ($(date +%Y-%m-%d))"
            echo ""
            
            # Process Random Access results
            if [ -f "./results/random_access_${{ env.TIMESTAMP }}.txt" ]; then
              echo "Random Access Test:"
              grep "Average time per random access:" "./results/random_access_${{ env.TIMESTAMP }}.txt" | head -n 1
            fi
            
            # Process Read Window results
            if [ -f "./results/read_window_${{ env.TIMESTAMP }}.txt" ]; then
              echo -e "\nRead Window Test:"
              grep "1000000 entries: Approx" "./results/read_window_${{ env.TIMESTAMP }}.txt" | head -n 1 | sed 's/^[[:space:]]*//'
              grep "1000000 entries: Exact" "./results/read_window_${{ env.TIMESTAMP }}.txt" | head -n 1 | sed 's/^[[:space:]]*//'
            fi
            
            # Process Index results
            if [ -f "./results/index_${{ env.TIMESTAMP }}.txt" ]; then
              echo -e "\nIndex Benchmark:"
              grep "End-to-end throughput:" "./results/index_${{ env.TIMESTAMP }}.txt" | head -n 1 | sed 's/^[[:space:]]*//'
            fi
            
            # Process General benchmark results
            if [ -f "./results/benchmark_${{ env.TIMESTAMP }}.txt" ]; then
              echo -e "\nGeneral Benchmark:"
              grep "Read test done" "./results/benchmark_${{ env.TIMESTAMP }}.txt" | head -n 1 | sed 's/^Read test done in.*: //'
            fi
          } > ./results/slack_summary.md

          CONDENSED_SUMMARY=$(cat ./results/slack_summary.md | jq -aRs .)
          echo "summary=$CONDENSED_SUMMARY" >> $GITHUB_OUTPUT

      - name: Send to Slack
        if: github.event_name == 'schedule'
        uses: slackapi/slack-github-action@v1.25.0
        with:
          channel-id: "tidehunter" # Replace with your channel ID
          payload: |
            {
              "text": "Weekly Benchmark Results",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "📊 Weekly Benchmark Results"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ${{ steps.condensed_summary.outputs.summary }}
                  }
                },
                {
                  "type": "context",
                  "elements": [
                    {
                      "type": "mrkdwn",
                      "text": "Run on: ${{ env.TIMESTAMP }} • <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View full results>"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
