[package]
name = "wal_inspector"
version = "0.1.0"
edition = "2021"

[dependencies]
tidehunter = { path = "../../tidehunter", features = ["test-utils"] }
anyhow = "1.0"
clap = { version = "4.0", features = ["derive"] }
hex = "0.4.3"

[dev-dependencies]
tempfile = "3.0"